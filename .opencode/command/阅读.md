---
description: 重读 AGENTS.md 关键章节、按任务挑选 skills 加载，并在改完文件后自动 commit + push + 部署
---

# /阅读

触发本仓库 AGENTS.md 顶部「触发词：「阅读」」章节定义的标准流程。

## 必做的 4 件事

### 1. 重读 AGENTS.md 关键章节

用 `read` 工具重新通读以下章节（高频违规重灾区）：

- **§0** 接到需求后的标准工作流
- **§3** AI 操作规则（特别是 §3.5 自动提交流程）
- **§5** 代码定位速查表
- **§6** 常见任务决策树
- **§9** 反复出现的坑

读完后用一句中文告诉用户：「已重读 §0 / §3 / §5 / §6 / §9」。

### 2. 根据任务挑选 skills

参考用户附带的任务描述（或最近一条任务）和 AGENTS.md §0.5 表格，列出本次要加载的 skills：

- **项目专属**（`mobile-ui-fix` / `desktop-ui-fix` / `pages-deploy-troubleshoot`）按场景**最多选 1 个**。
- **通用设计**（`ui-design-brain` / `color-system` / `typography-scale` / `spacing-system` / `visual-hierarchy` / `layout-grid` / `responsive-design` / `data-visualization` / `dark-mode-design`）按需要选 0–3 个，**不要全选**。
- **框架/工具类**（`vue` / `vue-best-practices` / `nuxt` / `pinia` / `cloudflare` / `wrangler` / `vitest` 等）只在任务确实涉及该领域时选。

### 3. 用 `skill` 工具实际加载

不是只列名字，要真的调用 `skill` 工具加载选出的 skills。加载完后再开始执行任务。

### 4. 任务结束前自动提交并部署

只要本轮**触碰过任何文件**就必须按 §3.5 一条龙执行（纯问答 / 纯解释除外）：

```powershell
git add -A
git commit -m "<简明中文描述>"
git push origin master
powershell -ExecutionPolicy Bypass -File ./scripts/deploy-pages.ps1
```

- `git push` 会触发 Cloudflare Pages 自动构建。
- `deploy-pages.ps1` 用 Wrangler 重新发布当前 commit 并验证生产地址，避免 CF Git 集成停在 `Idle` / `Skipped`（§3.6 反模式 #18）。
- 结尾在回复里按 §3.7 列出：触碰文件、验证结果、commit hash、是否多余操作。

## 没有具体任务时

如果命令调用时没附带任务描述：

- 完成第 1 步重读
- 列出 §0.5 全部可用 skills 让用户挑
- **不主动加载任何 skill**
- **也不执行第 4 步**
- 等用户给具体任务再触发第 2、3、4 步

## 反模式（不要做）

| ❌ | ✅ |
| --- | --- |
| 一次性 load 8 个 skill | 按任务挑 1–3 个 |
| 跳过重读 AGENTS.md，直接问"要我做什么" | 必须先重读，再问 |
| 重读完不汇报、直接进入下一步 | 必须用一句话告诉用户已重读 + 加载了哪些 skill |
| 改完文件就结束，等用户提醒才 commit | 改完文件 → `add` / `commit` / `push` / `deploy-pages.ps1` 一条龙跑完 |
| 只 `git push` 不跑 `deploy-pages.ps1` | 必须跑脚本验证生产指向当前 commit |

## 用户的任务描述

$ARGUMENTS
